import 'package:flutter/material.dart';

class LokasiSales extends StatelessWidget {
  const LokasiSales ({super.key});

  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
        title: const Text('Lokasi Sales'),
      ),
        body: Center(
          child: Text('Cooming Soon'),
        ),
      );
    }
}